FROM python:3.14-slim

WORKDIR /app

# Installa dipendenze
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt pytest

# Copia tutto il progetto
COPY . .

# Esegue i test di default
CMD ["python", "-m", "pytest", "tests/", "-v"]
