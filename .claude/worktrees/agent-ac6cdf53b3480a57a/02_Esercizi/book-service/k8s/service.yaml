apiVersion: v1
kind: Service
metadata:
  name: book-service
  namespace: book-service
  labels:
    app: book-service
spec:
  type: ClusterIP
  selector:
    app: book-service
  ports:
    - name: http
      port: 8000
      targetPort: 8000
